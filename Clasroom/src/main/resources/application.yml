spring:
  application:
    name: msvc-subject
  datasource:
    url: jdbc:postgresql://localhost:5432/SchoolSystem
    username: postgres
    password: root
  config:
    import: optional:configserver:http://localhost:8888
  jpa:
    show:
      sql: true
    properties:
      hibernate:
        format_sql: true
